# Normalise line endings. This repo is authored on Windows and built on Linux
# (Vercel); without this, every file round-trips as CRLF and diffs become noise.
* text=auto eol=lf

# Batch files must keep CRLF or cmd.exe mis-parses multi-line blocks.
*.bat text eol=crlf
