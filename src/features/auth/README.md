# features/auth

Sessions and identity. Owns sign up, sign in, sign out, the Supabase server
client, and the `requireUser()` helper every other feature calls to get a
trusted `userId`.

Built in phase 3.
